# DevDeck Run 2026-06-17T07-28-22-659Z-h8h4ih
- Date: 2026-06-17T07:28:22.660Z
- Host: `cli`
- Mode: `ask`
- Executor: `gemini`
- Agent: `gemini`
- Exit Code: 0
- Context Chars: 4971

## Task
```text
简单回答：DevDeck smoke test
```

## Output
DevDeck 的 **smoke test**（冒烟测试）通常包含以下几个关键步骤，用于验证核心流程（MCP、CLI 和路由）是否正常工作：

### 1. 验证基础配置
首先检查环境和已配置的执行器：
```bash
devdeck doctor
```
该命令会检查 `.devdeck/config.toml` 是否存在，以及各 Agent 命令行工具是否在 PATH 中。

### 2. 检查执行器列表
确认路由逻辑能正确加载配置：
```bash
devdeck list
```

### 3. 运行 Mock 路由测试
使用内置的 `mock` 代理进行端到端测试，这不会产生实际的 API 消耗：
```bash
devdeck ask mock "ping"
```
如果成功，你会看到包含 `Mock executor: mock` 和 `Task: ping` 的输出。

### 4. 验证运行日志
检查最近一次运行的结果是否被正确记录并可以读取：
```bash
devdeck last
```

### 5. (可选) 验证 MCP 服务
如果在 Codex 或其他支持 MCP 的客户端中使用，可以尝试启动服务（本地调试时通常由客户端自动启动）：
```bash
devdeck mcp start
```

**简单总结：** 跑一遍 `devdeck doctor` 和 `devdeck ask mock "test"` 即可确认 DevDeck 的核心逻辑是否打通。


## Errors
```text
Warning: Basic terminal detected (TERM=dumb). Visual rendering will be limited. For the best experience, use a terminal emulator with truecolor support.
Warning: 256-color support not detected. Using a terminal with at least 256-color support is recommended for a better visual experience.
Ripgrep is not available. Falling back to GrepTool.

```