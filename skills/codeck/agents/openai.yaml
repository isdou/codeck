interface:
  display_name: "Codeck 外部模型技能"
  short_description: "留在 Codex，按需调用你点名的模型"
  default_prompt: "Use $codeck to call the external Agy/AGY, Antigravity, Gemini, Claude, Kimi, or other model I explicitly named. Never create an internal Codex subagent with that name. Pass only the context needed and bring the result back to Codex."
